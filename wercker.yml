box: elixir:1.6-alpine

build:
  steps:
    - script:
        name: deps
        code: |
          mix local.hex --force
          mix deps.get

    - script:
        name: test
        code: mix coveralls.detail
